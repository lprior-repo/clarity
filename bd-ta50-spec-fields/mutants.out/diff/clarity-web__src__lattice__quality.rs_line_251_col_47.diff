--- clarity-web/src/lattice/quality.rs
+++ replace && with || in calculate_completeness
@@ -243,17 +243,17 @@
 
   let total_required = required_patterns.len();
 
   let filled_count = required_patterns
     .iter()
     .filter(|pattern| {
       answers
         .iter()
-        .any(|a| a.step_id.contains(*pattern) && !a.value.trim().is_empty())
+        .any(|a| a.step_id.contains(*pattern) || /* ~ changed by cargo-mutants ~ */ !a.value.trim().is_empty())
     })
     .count();
 
   // Check for empty required fields
   for pattern in &required_patterns {
     let has_answer = answers
       .iter()
       .any(|a| a.step_id.contains(pattern) && !a.value.trim().is_empty());
