--- clarity-web/src/lattice/quality.rs
+++ delete ! in calculate_completeness
@@ -253,17 +253,17 @@
     .count();
 
   // Check for empty required fields
   for pattern in &required_patterns {
     let has_answer = answers
       .iter()
       .any(|a| a.step_id.contains(pattern) && !a.value.trim().is_empty());
 
-    if !has_answer {
+    if  /* ~ changed by cargo-mutants ~ */has_answer {
       issues.push(QualityIssue::new(
         QualityDimension::Completeness,
         IssueSeverity::Error,
         format!("Missing required field: {pattern}"),
       ));
     }
   }
 
