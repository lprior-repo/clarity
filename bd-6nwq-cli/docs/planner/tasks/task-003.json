{
  "id": "task-003",
  "title": "state: define AntithesisResponse struct",
  "type": "feature",
  "priority": 0,
  "effort": "15min",
  "description": "Define the AntithesisResponse struct for storing 3 null hypothesis points with quality score",
  "ears": {
    "ubiquitous": [
      "THE SYSTEM SHALL always have exactly 3 antithesis points",
      "THE SYSTEM SHALL validate quality_score is between 0 and 1"
    ],
    "event_driven": [
      {
        "trigger": "WHEN user submits antithesis points",
        "shall": "THE SYSTEM SHALL calculate quality score based on specificity"
      }
    ],
    "unwanted": [
      {
        "condition": "IF quality_score exceeds 1.0",
        "shall_not": "THE SYSTEM SHALL NOT accept invalid quality scores",
        "because": "Invalid scores would break downstream validation logic"
      }
    ]
  },
  "contracts": {
    "preconditions": [
      "state.rs file exists",
      "Serde is available as dependency"
    ],
    "postconditions": [
      "AntithesisResponse has points: [String; 3] field",
      "AntithesisResponse has quality_score: f64 field",
      "Default impl provides empty strings and 0.0 score"
    ],
    "invariants": [
      "points array always has exactly 3 elements",
      "quality_score is clamped to 0.0..1.0 range"
    ]
  },
  "tests": {
    "happy": [
      "Default::default() creates empty antithesis with 0.0 score",
      "Can create with custom points and score"
    ],
    "error": [
      "Quality score 1.5 is clamped to 1.0",
      "Quality score -0.5 is clamped to 0.0"
    ],
    "edge": [
      "Serialization preserves all 3 points",
      "Clone creates independent copy of array"
    ]
  },
  "implementation": {
    "phase_0": [
      "Review Rust array patterns for fixed-size arrays"
    ],
    "phase_1": [
      "Write test_default_antithesis_is_empty",
      "Write test_quality_score_is_clamped"
    ],
    "phase_2": [
      "Define AntithesisResponse struct with points and quality_score",
      "Implement Default trait",
      "Add validation method for quality score clamping",
      "Add Serialize/Deserialize derives"
    ]
  },
  "context": {
    "related_files": [
      "clarity-web/src/components/discover/state.rs"
    ],
    "similar": [
      "See QualityScore struct in lattice/quality for score patterns"
    ]
  }
}
