{
  "id": "task-002",
  "title": "state: define ConfirmSubPhase enum",
  "type": "feature",
  "priority": 0,
  "effort": "15min",
  "description": "Define the ConfirmSubPhase enum with all 5 sub-states for field-by-field confirmation wizard",
  "ears": {
    "ubiquitous": [
      "THE SYSTEM SHALL always track current confirmation sub-phase",
      "THE SYSTEM SHALL provide Default impl returning Problem sub-phase"
    ],
    "event_driven": [
      {
        "trigger": "WHEN user completes Problem field confirmation",
        "shall": "THE SYSTEM SHALL transition to Persona sub-phase"
      },
      {
        "trigger": "WHEN user clicks Back button",
        "shall": "THE SYSTEM SHALL transition to previous sub-phase"
      }
    ],
    "unwanted": [
      {
        "condition": "IF back is pressed on first sub-phase",
        "shall_not": "THE SYSTEM SHALL NOT go to negative index",
        "because": "Going before first field would break the wizard flow"
      }
    ]
  },
  "contracts": {
    "preconditions": [
      "ProgressiveDiscoverPhase enum exists",
      "state.rs file has ConfirmSubPhase placeholder"
    ],
    "postconditions": [
      "ConfirmSubPhase enum has exactly 5 variants",
      "next() method returns Option<ConfirmSubPhase>",
      "prev() method returns Option<ConfirmSubPhase>"
    ],
    "invariants": [
      "Sub-phases are ordered: Problem, Persona, Solution, Nonpersona, Scenario",
      "First sub-phase has no previous, last has no next"
    ]
  },
  "tests": {
    "happy": [
      "Default::default() returns ConfirmSubPhase::Problem",
      "Problem.next() returns Some(Persona)"
    ],
    "error": [
      "Scenario.next() returns None",
      "Problem.prev() returns None"
    ],
    "edge": [
      "Can iterate through all sub-phases using next()",
      "Can iterate backwards using prev()"
    ]
  },
  "implementation": {
    "phase_0": [
      "Review existing iterator patterns in Rust std library"
    ],
    "phase_1": [
      "Write test_default_subphase_is_problem",
      "Write test_next_transitions_correctly",
      "Write test_prev_returns_none_at_start"
    ],
    "phase_2": [
      "Define ConfirmSubPhase enum with Problem, Persona, Solution, Nonpersona, Scenario",
      "Implement Default trait",
      "Implement next() method returning Option<Self>",
      "Implement prev() method returning Option<Self>",
      "Add Display trait"
    ]
  },
  "context": {
    "related_files": [
      "clarity-web/src/components/discover/state.rs"
    ],
    "similar": [
      "See Option pattern for next/prev navigation"
    ]
  }
}
